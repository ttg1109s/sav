# Changelog — Ver 12: Hạ tầng rẽ nhánh theo `appState` cho `/event/` (nay đã wire thật, không còn nằm im), Đa phương tiện (Slideshow/Ảnh/Documents v1, IndexedDB v4), tái cấu trúc Settings + Theme (4 mode), Subtitle Editor tách thành trang riêng, Documents viết lại hoàn chỉnh lần 2 (Nhóm A), Rule 5 cho hàm dựng UI.

> **[CẬP NHẬT 12/07/2026]** Bản trước của file này CHỈ ghi lại đúng 1 phiên (hạ tầng
> `operation.js`/`block.js`/`virtual-machine-state.js`, mục 1-2 dưới đây — GIỮ NGUYÊN không sửa,
> xem ghi chú cuối mục 2) — toàn bộ khối lượng việc làm SAU đó (đa phương tiện, tái cấu trúc
> Settings/Theme, Subtitle Editor, Documents Nhóm A) chưa từng được gộp vào file changelog chính
> thức nào, chỉ nằm rải trong 3 bản `sav12-handoff-plan*.md` (tài liệu bàn giao tạm giữa các phiên,
> không phải changelog chính thức). Bản cập nhật này đối chiếu LẠI TRỰC TIẾP VỚI CODE THẬT (không
> chỉ chép lại 3 bản handoff) — 2 chỗ ghi SAI trước đó đã được đính chính, xem mục 3 (bản GỐC của
> chính file này) và mục "Nợ kỹ thuật để lại" cuối bài (bản `sav12-handoff-plan.md` 09/07, nhầm
> "Fix B" shuffle là đã xong).

---

## 1. Vấn đề xuất phát

Kiến trúc `/event/` (`listener → bus → router.handle(msg) [switch theo msg.type] → workflow/core`)
chưa có chỗ CHUẨN cho 2 nhu cầu rẽ nhánh theo `appState` (khác `msg.payload` của chính message):

- **Chặn hẳn 1 hành vi khi đang ở 1 state nào đó** — trước đây viết tay if/else ngay trong core
  hoặc router mỗi nơi 1 kiểu, dễ lệch nhau khi CÙNG 1 điều kiện cần dùng ở NHIỀU router khác nhau
  (case thật phát hiện: nút "đổi kiểu hiệu ứng" tới từ cả nút cycle (Control Center) lẫn select
  (Settings) — cycle có kiểm tra `autoSwitchVisualEnabled` bên trong core, select thì KHÔNG, dẫn
  tới hành vi khác nhau giữa 2 entry point cùng 1 ý nghĩa nghiệp vụ).
- **Chạy nhiều workflow độc lập theo nhiều điều kiện state khác nhau, không loại trừ nhau** (vd
  điều kiện A khớp chạy workflow1, điều kiện B khớp chạy workflow2, A và B CÙNG khớp thì CẢ HAI
  cùng chạy) — không mô tả được gọn bằng switch/if thường (switch chỉ chọn 1 nhánh).

## 2. Giải pháp — 2 cơ chế tách biệt, dùng chung 1 bộ toán tử

**`service/operation.js`** — `operation.evaluate(a, operator, b)`, trả boolean. Hỗ trợ đủ toán tử:
`=== !== > < >= <= in notIn` (`in`/`notIn` dựng `Set` tạm tra O(1) thay vì `Array.includes()`
O(n), `.clear()` ngay sau khi lấy kết quả). Không biết gì về `appState`/`msg`/callback — thuần so
sánh 2 giá trị. Dùng chung cho cả 2 cơ chế dưới, tránh 2 nơi tự định nghĩa 2 bảng toán tử lệch nhau.

**`event/bus.js` (mở rộng) + `event/block.js`** — CHẶN NHỊ PHÂN 1 `msg.type` TRƯỚC KHI
`router.handle()` được gọi. `eventBus` thêm `blocks` Map + `registerBlock(msgType, groups)` +
`evalCondition()` (field đọc qua `appState`, hỗ trợ path lồng bất kỳ độ sâu vd
`'vizConfig.autoSwitchVisualEnabled'`) + check trong `send()`. `groups` là mảng NHÓM — OR giữa
nhóm, AND trong 1 nhóm. Đăng ký thật nằm ở `event/block.js` (file DATA riêng, `bus.js` chỉ giữ cơ
chế) — **hiện ĐANG RỖNG**, chưa có entry nào. CHỈ làm được chặn/không chặn (boolean) — KHÔNG chọn
được "workflow nào chạy" (việc đó vẫn thuộc router hoặc `virtual-machine-state`, xem dưới), giữ
đúng nguyên tắc gốc của bus: *"không biết gì về nghiệp vụ"*.

**`event/virtual-machine-state.js`** — `VirtualMachineState.run(rules)`, gọi NGAY TRONG 1 case của
router (không phải trước router như block). KHÔNG có registry/Map — `rules` truyền trực tiếp tại
chỗ gọi, domain tự nhiên vì nằm sẵn trong file router sở hữu nó. File này KHÔNG biết gì về
`appState`/`msg`/`eventBus`/core/workflow — router tự đọc state, tự đóng gói `callback`. Chạy
**TẤT CẢ** rule có điều kiện khớp, KHÔNG dừng ở rule đầu tiên (khác `if/else if`) — 2 rule cùng
khớp thì cả 2 callback đều chạy, đúng thiết kế yêu cầu.

> **[Đính chính 12/07/2026]** Câu "hiện ĐANG RỖNG, chưa có entry nào" ở mục 2 trên GIỮ NGUYÊN
> đúng như lúc viết (đúng tại thời điểm đó) — nhưng **không còn đúng với code hiện tại**. Xem mục 3
> ngay dưới để biết đã wire thật ra sao.

---

## 3. Đã wire thật vào code — không còn "nạp mà chưa dùng"

Phần "Nợ kỹ thuật để lại cho phiên sau" ở bản gốc từng ghi "**Chưa wire case thật nào**". Rà lại
code thật (`grep` toàn bộ `VirtualMachineState.run(` + `registerBlock(`) xác nhận điều đó **không
còn đúng** — cả 2 cơ chế đã được dùng thật, rải qua nhiều phiên sau đó:

- **`event/block.js` có 1 entry thật** (03/07/2026, đợt 4 File Manager Song):
  `fileManagerSong.folder.applyToPlaylist.click` bị chặn khi `folderDetailSongCount === 0` (đọc qua
  `appState`, cập nhật mỗi lần vẽ lại Folder Detail Drawer) — đúng use-case mẫu mà mục 1 đã nêu
  (chặn hẳn 1 hành vi theo state, dùng được ở bất kỳ router nào cần). Comment đầu file
  `event/block.js` ("[Khung mới tạo — CHƯA có entry nào đăng ký]") giờ đã LỖI THỜI, chỉ còn đúng ý
  nghĩa "khung was mới tạo lúc viết", không phải hiện trạng — nên sửa lại nếu file đó bị đụng tới
  lần sau (không nằm trong phạm vi bản cập nhật changelog này).
- **`VirtualMachineState.run()` đã dùng rộng** ở nhiều router/workflow/core khác nhau:
  `event/router/file-manager-photo.js` (3 chỗ), `event/router/playlist-empty-state.js` (2 chỗ),
  `event/router/playlist.js` (3 chỗ), `event/router/file-manager-song.js` (2 chỗ, có 1 lồng ngay
  trong callback của 1 rule khác), `event/workflow/playlist.js` (3 chỗ),
  `core/visualizer/draw-visualizer.js` (hot path, 1 chỗ), `core/file-manager/folder.js`
  (`addSongsToFolder()`, 1 chỗ). Tổng cộng ≥15 điểm gọi thật, xác nhận 2 cơ chế mục 1-2 đã qua giai
  đoạn "hạ tầng chưa dùng" sang "hạ tầng đang chạy thật trong production code".

Đây chính là đợt "thực nghiệm" mà kế hoạch ban đầu ngầm định khi thêm hạ tầng này — dùng đúng 1 bug
nhỏ có thật (folder rỗng vẫn bấm "Áp dụng" được) làm ca đầu tiên trước khi lan ra các cụm khác.

## 4. File Manager chuyển hẳn vào Settings (03/07/2026) — bối cảnh cho mục 3

`components/settings/file-manager-section.js` (MỚI) — File Manager KHÔNG còn là overlay/drawer
cấp cao riêng như patch 02/07 trước đó nữa, mà là 1 section BÌNH THƯỜNG trong Settings, cùng dạng
card với các section khác. 3 hàng con (Song / Ảnh & Album / Documents) bấm vào PUSH THẲNG sang
drawer con tương ứng (`components/file-manager.js`), đúng pattern nav-stack đã có sẵn cho About —
không có màn "File Manager" trung gian nào nằm giữa nữa. Thêm 1 hàng "Dọn rác" cuối section, cố ý
đặt sau mọi tính năng khác (`core/file-manager/cleanup.js` + `event/workflow/file-manager-cleanup.js`,
registry `registerCleanupCheck()`). Tái sắp xếp lại 07/07/2026 — dời xuống sau "Phụ đề" (không còn
ngay sau "Playlist" như bản 03/07).

IndexedDB nâng `DB_VERSION` 3 → 4 (`service/db.js`), thêm 5 store mới: `folders`, `folder_song`,
`images`, `albums`, `documents` — hạ tầng cho File Manager Song (folder/scope playlist),
Photo/Album (nền slideshow), và Documents (xem mục 5).

## 5. Nhóm B/C (một phần) — Đa phương tiện: Slideshow, Ảnh, Documents bản v1 (02–05/07/2026)

Trước khi Nhóm A (mục 8) viết lại Documents lần 2, đã có 1 bản v1 chạy được:

- **Slideshow nền:** engine chuyển ảnh 13 kiểu CSS transition + Ken Burns (`core/file-manager/
  slideshow.js`, `assets/css/slideshow.css`), chế độ "ảnh theo từng bài hát", overlay caption (lúc
  đó CHƯA phát hiện ra `#bg-caption-frame`/`#bg-caption-text` thiếu HTML thật — xem "Nợ kỹ thuật để
  lại" cuối bài, việc này TỚI GIỜ (12/07) vẫn còn treo y nguyên).
- **Masonry photo grid** — tải/thu gọn theo chunk, `core/file-manager/image.js` +
  `core/file-manager/photo-ui.js`; carousel picker chọn nhiều ảnh 1 lượt.
- **Documents bản v1** (Markdown + Toast UI Editor CDN + Turndown — hướng đi ĐÃ BỊ THAY THẾ hoàn
  toàn ở Nhóm A, mục 8 dưới — nhắc lại ở đây chỉ để biết vì sao `plan.txt`/`plan-v12-extended.md`
  mục 1 mở đầu bằng câu "thay thế hướng đi Markdown/Toast UI Editor/Turndown").
- **File Manager cleanup registry** — `registerCleanupCheck()` (`core/app-cleanup.js`), dọn
  reference orphan (ảnh/document không còn bài nào trỏ tới).
- **Multi-select Playlist** — chọn nhiều bài (checkbox), dropup action menu.
- Responsive: Playlist/Visualizer đổi từ trượt dọc sang đẩy ngang trên mobile, split-column trên
  desktop.

## 6. Nhóm D — Tái cấu trúc Settings + Theme (05–09/07/2026)

**Settings restructure (D1→D7, batch riêng từng đợt qua nhiều ngày):**
- Toàn bộ Settings dồn về 1 core UI dùng chung — `core/settings-panel-stack.js` — ngăn xếp
  (stack) panel, push/pop điều hướng Main → Song/Photo/Document/Folder Detail...
- **2 lần viết lại animation cùng 1 file** (đọc thẳng từ comment đầu file, không suy đoán):
  - Bản 06/07: panel mới trượt CHE LÊN panel cũ (`position: absolute`, tự animate `left`).
  - **HOTFIX 17 (08/07, Giang chốt):** đổi hẳn sang cuộn ngang THẬT (`overflow-x` +
    `scrollTo()`) — cùng pattern đã ổn định cho `#side-left-container` (Playlist↔Settings) — vì
    animate `left` thủ công "luôn tiềm ẩn xung đột với animation/rendering khác" (nguyên văn comment
    file, đúc kết từ chuỗi bug HOTFIX 12-16 trước đó).
  - **09/07 — rút hàm dùng chung:** `core/slider-panel-scroll.js` (`getPositionStart()`/
    `scrollSliderTo()`), dùng CHUNG bởi `settings-panel-stack.js` VÀ `core/player-controls.js` (2
    nơi từng tự viết `scrollTo()` riêng, giờ gộp 1).
- Mỗi item Settings thêm icon đứng trước label; label dài chặn vỡ layout bằng
  `whitespace-nowrap overflow-hidden text-ellipsis`.

**Theme (mới, chỉ phần CHỌN, CHƯA áp màu thật):**
- `components/settings/theme.js` — 4 card LOẠI TRỪ NHAU (radio): Sáng, Tối (mặc định), Background
  (ảnh nền tuỳ chỉnh, tái dùng hệ `bgImage`/`bgBlur` có sẵn), **Gradient** (09/07, MỚI ngoài dự kiến
  gốc của plan — `linear-gradient(135deg, gradientFrom, gradientTo)`, field mới
  `vizConfig.gradientFrom`/`gradientTo` — **PHẢI khai ở CẢ `core/config.js` LẪN
  `service/state.js`** — bài học "2 bản `DEFAULT_VIZ_CONFIG` độc lập, không tự đồng bộ" đã có từ
  trước, bỏ sót 1 chỗ sẽ ra `undefined` ở lần khởi tạo thật đầu tiên).
- Card Background/Gradient tự phản ánh ảnh/gradient đang chọn thật (mockup preview động, không còn
  hình tĩnh) — `event/workflow/theme.js::refreshThemeCardUI()`.
- Indicator chọn đổi từ viền (`ring-2 ring-sky-400`) sang nền gradient phủ lên card đang chọn.
- **"Sáng" (Light) mới chỉ LƯU lựa chọn, CHƯA áp lại màu app thật** — đây là hạng mục LỚN NHẤT của
  cả Nhóm D theo đánh giá của `plan.txt` mục 7 (đổi hàng chục file `components/*.js` sang cặp
  `dark:`/mặc định) — **VẪN TREO tới 12/07/2026**, chưa có `/theme/light/tokens.js` +
  `/theme/dark/tokens.js` như cấu trúc `plan.txt` đề xuất.

**2 bug hotfix khác trong đợt này:** `openImageCarouselPickerModal()` (callback-based, không trả
Promise) gây kẹt UI lúc chọn ảnh nền — bọc `Promise` quanh nó trong `toggleBgImage()`
(`event/workflow/visualizer-display.js`); `forceGlassRepaint()` (`core/color-utils.js`) fix nền cũ
kẹt lại do WebKit compositor cache `backdrop-filter` stale, gọi sau mọi `updatePlaylistBg()` chủ
động (6 điểm gọi).

## 7. Subtitle Editor — từ modal `#subtitle-modal` sang trang riêng `subtitle-editor.html` (10–11/07/2026)

**Đổi hướng lớn (10/07, phản hồi Giang):** không còn sửa phụ đề qua modal trong `index.html` —
tách hẳn thành `subtitle-editor.html`, mở qua query string `?song=<key đã mã hoá>`
(`service/song-key-cipher.js::encodeSongKeyForUrl()`/`decodeSongKeyFromUrl()`). Trang này KHÔNG nạp
lại `index.html`/`main.js` — tự khai script riêng: Tailwind CDN, **WaveSurfer.js** (thư viện thứ 3,
thay tự viết Worker decode — "nếu phức tạp thì dùng thư viện thứ 3"), `service/db.js` (CHUNG
IndexedDB, cùng origin `file://`), `lang.js`+patch, `event/bus.js`, `core/modal-choice.js`,
`core/subtitle/subtitles.js`+`subtitles-ui.js`, cụm `event/{workflow,router,listener}/
subtitle-editor.js`. Component cũ `components/subtitle-modal.js` đã XOÁ khỏi `index.html`.

**Bộ công cụ dựng thêm trong trang mới:** waveform WaveSurfer + vùng chọn (region) kéo tay, Split,
Cut MP3 (encoder JS thuần `lamejs`, mã hoá trực tiếp buffer PCM đã giải mã của WaveSurfer, KHÔNG
cần server), Shift (dời toàn bộ mốc thời gian), auto-timing, time-picker modal, dải mốc thời gian
(plugin Timeline chính thức của WaveSurfer, không tự vẽ tay), zoom. Model chỉnh sửa đổi từ
auto-commit-on-blur sang chế độ Sửa tường minh (Apply/Cancel), đồng bộ 2 chiều region↔pending, cache
`_lineCardNodesById` Map bền (theo đúng pattern `renderPlaylistDiff()` đã có).

**Root cause đã fix trong đợt "chuyển giao vừa xong":** `service/db.js` từng cache kết nối IndexedDB
vào `appState` — biến này KHÔNG được nạp ở trang `subtitle-editor.html` (trang không load
`main.js`), gây init âm thầm fail không lỗi hiển thị. Cùng đợt: `manualEq` array bị đóng băng
(frozen) nhầm, race condition ở preloader, và loạt bug tương thích WaveSurfer v7.

**Bug đã biết, ĐÃ CẢNH BÁO trong code (chưa xác nhận đã hết):** WaveSurfer v7 có lỗi công khai
(GitHub issue #3126 katspaugh/wavesurfer.js) khiến lỗi giải mã audio đôi khi không đi qua sự kiện
`'error'` mà bắn `unhandledrejection` trực tiếp — trang này bắt CẢ 2 (`window.onerror` +
`unhandledrejection`) ngay đầu `<head>`, sớm hơn cả Tailwind/WaveSurfer, kèm bộ thu log hiện trên
màn hình (không cần console trình duyệt, vì thiết bị di động/WebView thường không có).

> **Nợ kỹ thuật mới phát sinh chính từ trang này — xem mục 10.**

## 8. Nhóm A — Documents: viết lại HOÀN TOÀN, đổi hướng content model giữa phiên (10–11/07/2026)

Nhóm A (mục 1-4 của `plan.txt`/`plan-v12-extended.md`) từng đứng yên "CHƯA LÀM GÌ" suốt từ 05/07 tới
09/07 (xác nhận qua 2 bản handoff-plan trước) — phiên 10-11/07 code xong toàn bộ 7 bước, kèm **1 lần
đổi hướng lớn giữa chừng** so với chữ viết gốc của plan:

- **`core/file-manager/document.js`** — bản ĐẦU phiên theo sát `plan.txt` (".txt → tách đoạn → bọc
  `<p>` → 1 chuỗi HTML ngay lúc lưu") hoá ra SAI theo phản hồi Giang. **Bản CUỐI (đang chạy)**:
  `.txt` upload lưu THẲNG `string[]` (không convert HTML lúc lưu, `string[]` là định dạng CHÍNH
  THỨC cho trường hợp này chứ không phải "tương thích ngược" như hiểu nhầm ban đầu); `.docx`
  (mammoth.js) và mọi tài liệu `createdBy === 'user'` (sửa qua contentEditable) lưu `string` HTML đã
  `sanitizeDocumentHtml()` (whitelist `h1-h6/p/b/strong/i/em/u/blockquote/ul/ol/li/a`, đệ quy
  bottom-up, unwrap tag lạ, xoá attribute thừa — chỉ `<a>` giữ `href`). `resolveDocumentHtml()` bọc
  `<p>` cho `string[]` CHỈ LÚC ĐỌC. Tải về LUÔN ra `.txt` qua `convertDocumentHtmlToPlainText()`
  (MỚI, thay Turndown) — KHÔNG phải `.html` như bản đầu phiên từng làm sai (Giang bắt lỗi này).
- **`core/generic-drawer.js` + `components/generic-drawer.js`** — khung dùng chung List↔Read cho
  Document (chuyển trạng thái qua `updateGenericDrawer()`, không đóng/mở lại từ đầu). **ĐÃ BỎ HẲN
  lớp overlay** (bug: đóng xong không thao tác được gì trên UI vì `closeGenericDrawer()` cũ không
  bao giờ thêm lại `hidden` cho overlay — Giang xác nhận lớp này không cần thiết, bỏ hẳn thay vì chỉ
  vá timing).
- **`components/items.js`** — `itemTemplateDocumentRow()`/`renderItemList()`, gán `innerHTML` 1 LẦN
  duy nhất thay N lần `createElement`, đủ mượt tới ~100-200 item. Chỉ wire cho Document Picker,
  CHƯA đụng danh sách Song/Photo trong File Manager.
- **`core/file-manager/document-pagination.js`** (MỚI) — `computeNextDocumentReaderSlot()`, cắt
  HTML theo KHỐI (không cắt đôi 1 thẻ). **1 bug đo kích thước nghiêm trọng đã fix:**
  `pageSize.className` copy nguyên từ khung đọc thật (có `h-full`) sang `measureEl` là
  `position: fixed` — với phần tử `fixed`, `height: 100%` phân giải theo VIEWPORT chứ không theo ý
  muốn, khiến `scrollHeight` luôn "phồng" cỡ viewport → mọi khối sau khối đầu bị coi là tràn → 1
  trang chỉ có đúng 1 đoạn văn. Fix: ép `measureEl.style.height = 'auto'` (inline thắng class) ngay
  sau khi gán className. **Bài học chung:** bất kỳ phần tử ĐO TẠM nào copy `className` từ phần tử
  thật có `position` khác (fixed/absolute so với static/relative) đều có nguy cơ này.
- **`event/workflow/document-reader.js`** — gộp `workflowDocumentPicker` cũ vào đây (workflow
  `document-picker.js` cũ giờ chỉ còn nằm trên đĩa, không nạp — xem mục 11 nợ file mồ côi). Preload
  5 slot lúc mở tài liệu, VÀ giữ luôn 5 trang cache phía trước sau MỖI lần next/prev (không chỉ +1
  khi cạn cache như `plan.txt` viết ban đầu — Giang yêu cầu sửa thành preload liên tục). Hiệu ứng
  trượt trang: 2 lớp `position: absolute` chồng nhau, CSS transition 220ms, dọn qua `transitionend`.
- **Editor `contentEditable`:** toolbar Heading/Bold/Italic/Underline/Quote/Bullet/Numbered dùng
  `document.execCommand()`. Bug: `formatBlock` KHÔNG tự toggle như bold/italic (khác
  `queryCommandState`) — quote bấm lại chỉ áp lại blockquote, không thoát được. Fix
  `toggleQuote()` tự đọc tag khối hiện tại để quyết định BLOCKQUOTE↔P. Heading cycle cũng sửa: từ
  BẤT KỲ tag nào không phải H2/H3 (kể cả đang BLOCKQUOTE) → H2 trước (bản cũ rơi thẳng về P). Thêm
  active/inactive class cho toàn bộ nút toolbar.
- **Xoá:** `components/document-picker-drawer.js`, `components/document-reader.js` — đã xác nhận
  không còn tồn tại trên đĩa (`find` xác nhận). `index.html` gỡ CDN Toast UI Editor + Turndown.
- **Việc phát sinh KHÔNG có trong plan gốc:**
  - **Rule 5 mới** — xem mục 9.
  - **Fix hover kẹt trên cảm ứng, áp TOÀN APP:** `tailwind.config.future.hoverOnlyWhenSupported =
    true` (1 dòng, ngay sau CDN Tailwind) — `hover:*` giờ chỉ chạy trong
    `@media (hover: hover) and (pointer: fine)`, không còn bị cảm ứng giả lập kẹt.

**2 mục CHƯA giải quyết, ghi nhận lại từ handoff cuối phiên 11/07 (chưa có cập nhật mới hơn):**
- Bug nghi vấn "hàng Document trong File Manager không bấm được" — nghi lệch phiên bản
  `renderDocumentList()` giữa 2 đợt patch liên tiếp, đã gửi lại cả 2 file khớp nhau + bump cache,
  **CHƯA CÓ XÁC NHẬN từ Giang là đã hết**.
- Tính năng "Table of Contents" Giang nhắc tới trong Reader/Editor — grep toàn bộ project ra 0 kết
  quả, không tồn tại trong bản code đang có — vẫn đang CHỜ Giang cung cấp file/tên tính năng nguồn.

## 9. `readme/core-function-conventions.md` — Rule 5 (10/07/2026)

Đã cập nhật thẳng trong file (không cần lặp lại toàn bộ ở đây, xem file đó) — bổ sung Rule 5 cho
hàm dựng UI (trước đây vài file tự nhận "core UI thuần, ngoài phạm vi rule", TUYÊN BỐ ĐÓ SAI):
- **5a:** `addEventListener` cấm rải rác, CHO PHÉP nếu dựng ra cụm DOM MỚI (modal/drawer/toolbar
  tự `createElement`) — callback chỉ được gọi tham số, VÀ phải gom hết ở CUỐI hàm.
- **5b:** không dùng `classList`/`dataset`/`querySelector(...)` tồn tại hay không làm điều kiện rẽ
  nhánh nghiệp vụ — cùng vi phạm Rule 1, chỉ khác nguồn đọc là DOM thay vì `appState`.
- **5c:** file core dựng UI phải có hậu tố `-ui.js` trong tên. **Nợ kỹ thuật đã ghi nhận ngay khi
  thêm rule:** `core/modal-choice.js` vi phạm 5c (không có hậu tố dù có `createElement`) — chưa đổi
  tên, chỉ bắt buộc khi file đó bị đụng thật.

## 10. Nợ kỹ thuật MỚI — Seek trước rồi phát trong Subtitle Editor: tiến trình UI không theo kịp

**Vị trí:** `event/workflow/subtitle-editor.js` — `seekFromClick()`, `_playRangeAndStop()`,
`_startPlaybackWithRetry()`, `_seekWithRetry()`. **Triệu chứng Giang mô tả:** seek (bấm chọn vị trí
trên waveform, hoặc chọn 1 dòng/vùng) RỒI MỚI bấm phát — nhạc chạy đúng ÂM THANH, nhưng tiến trình
UI không theo kịp: thanh seek/current time không chạy đúng, icon play/pause của control bar sai
trạng thái, vùng chọn (region) và dòng phụ đề đang highlight không đồng bộ.

**Đã có nỗ lực fix trong code (chưa dứt điểm — Giang xác nhận triệu chứng còn):**
- Root cause đã xác định và ghi trong comment: pipeline vẽ sóng bắn sự kiện `'ready'` xong KHÔNG có
  nghĩa thẻ `<audio>` bên dưới đã thật sự sẵn sàng nhận lệnh seek — gọi `setTime()` trần trụi có thể
  bị ÂM THẦM BỎ QUA, đặc biệt lần ĐẦU TIÊN thao tác sau khi trang vừa load xong.
- Fix đã áp: `_seekWithRetry(time, attemptsLeft, onSeeked)` — seek rồi XÁC MINH THẬT bằng đọc lại
  `getCurrentTime()`, tự thử lại nếu chưa ăn, CHỈ gọi `onSeeked` (thường là `.play()`) sau khi xác
  nhận seek xong — thay vì "seek rồi phát ngay lập tức" như bản đầu.
- `_startPlaybackWithRetry()` — bắt Promise reject của `.play()` (va chạm với `pause()` vừa gọi) VÀ
  xác minh lại bằng `isPlaying()` sau 150ms, phân biệt rạch ròi "chưa từng chạy được tí nào"
  (`getCurrentTime() <= start + 0.05`) với "đã chạy xong rồi" — tránh gọi `.play()` thừa khiến nhạc
  phát tuột luôn tới hết bài (bug cũ đã fix riêng).
- `this._wavesurfer.on('timeupdate', ...)` cập nhật hiển thị thời gian + canh dừng phát theo
  vùng/dòng; `'play'`/`'pause'` cập nhật icon SAU KHI wavesurfer xác nhận đã chạy (không gọi ngay
  sau lệnh `.play()`, vì lúc đó `isPlaying()` có thể vẫn trả `false`).

**Kết luận:** cơ chế xác minh+retry đã có, nhưng THEO GIANG BÁO LẠI triệu chứng gốc (seek trước rồi
phát → tiến trình UI/region/subtitle line lệch) **vẫn còn tái diễn** — nghĩa là 1 trong các đường
seek/play CHƯA đi qua đủ `_seekWithRetry`/`_startPlaybackWithRetry`, hoặc còn 1 race khác ngoài 2
hàm này (vd handler `'timeupdate'` cũ bị gỡ sai lúc chuyển giữa các chế độ phát preview/chính). Ghi
nhận CHÍNH THỨC là nợ kỹ thuật — CHƯA điều tra sâu thêm trong bản cập nhật changelog này (việc đó
cần 1 phiên đọc lại toàn bộ `event/workflow/subtitle-editor.js` — file lớn nhất project, 1453
dòng — kèm test tay trên thiết bị thật, không patch mù).

## 11. Kiểm tra bug: 2 file mồ côi/lạc chỗ phát hiện qua rà soát toàn project

Rà bằng `grep`/`find` đối chiếu `index.html`/`subtitle-editor.html` (không suy đoán, đúng tinh thần
đã làm ở `changelog/v11.md` mục 1):

1. **`event/workflow/document-picker.js` — file mồ côi, KHÔNG còn nạp ở đâu.** Đã bị gộp vào
   `workflowDocumentReader` từ đợt Nhóm A (mục 8) — `index.html` dòng ~701 tự ghi chú "ĐÃ GỘP VÀO
   workflowDocumentReader" nhưng file vật lý (30 dòng) vẫn còn trên đĩa, không script nào trỏ tới,
   biến `workflowDocumentPicker` không còn được tham chiếu ở bất kỳ router/listener/workflow nào
   khác. Giữ nguyên tên hàm `open()` cũ dễ gây nhầm nếu ai đó tìm nhầm file lúc sửa Document Picker
   sau này (đúng loại lỗi mồ côi đã gặp ở ver 11). **Khuyến nghị: xoá hẳn file này.**
2. **`lang/patch/subtitle-editor.html` — file lạc chỗ, là 1 BẢN CŨ của `subtitle-editor.html`.**
   Thư mục `lang/patch/` lẽ ra CHỈ chứa file `.js` khai `key` i18n — file `.html` này (228 dòng,
   cache-bust `?v=20260710v2`) là snapshot CŨ của trang Subtitle Editor, thiếu hẳn plugin Timeline,
   nút "Cut MP3"/lamejs, nút tải lại không cache — toàn bộ tính năng thêm ở đợt 11/07 (xem mục 8) —
   so với bản THẬT đang chạy ở root `subtitle-editor.html` (`?v=20260711v3`). `grep` xác nhận **0
   tham chiếu** tới file này ở bất kỳ đâu trong project — không được nạp, không ai trỏ tới, thuần
   là rác lẫn vào sai thư mục (nhiều khả năng lẫn vào lúc đóng gói/giải nén 1 đợt patch trước).
   **Khuyến nghị: xoá hẳn file này** — không có giá trị tham khảo (bản root mới hơn và đúng hơn).

`node --check` chạy lại toàn bộ `.js` trong project (không giới hạn `core/`) — **sạch, không lỗi cú
pháp nào** ở bất kỳ file nào, kể cả 2 file mồ côi trên (mồ côi ≠ lỗi cú pháp).

## 12. Đánh giá dọn comment — dữ liệu để Giang tự quyết, CHƯA xoá gì

Không tự ý xoá/rút gọn comment nào trong bản cập nhật này (comment lịch sử quyết định là quy ước
Giang chủ động duy trì xuyên suốt project, không phải rác ngẫu nhiên) — chỉ đo đạc để có số liệu
thật nếu Giang muốn mở 1 đợt dọn riêng:

| File | Số dòng | Dòng comment | Tỉ lệ |
|---|---|---|---|
| `event/workflow/subtitle-editor.js` | 1453 (lớn nhất project) | 449 | 30% |
| `core/dom-refs.js` | 379 | 230 | **60%** |
| `core/auto-switch-visual.js` | 387 | 216 | **60%** |
| `service/db.js` | 340 | 197 | 57% |
| `core/player-controls.js` | 591 | 329 | 55% |
| `core/config.js` | 319 | 158 | 49% |

Tỉ lệ đo bằng regex dòng bắt đầu `*`/`//`/`/*` — cận trên (không phân biệt được comment "còn giá
trị tra cứu" với "lịch sử đã xong xuôi, không cần đọc lại nữa"), nhưng đủ để thấy 2 nhóm ứng viên rõ
rệt nếu muốn dọn: (a) file NHỎ nhưng tỉ lệ cao (`dom-refs.js`, `auto-switch-visual.js` — 60%, thuần
do comment giải thích nhiều hơn code thật) — dọn ít tốn công, lợi ích rà soát rõ; (b) file LỚN + tỉ
lệ cao (`player-controls.js`, `subtitle-editor.js`) — dọn tốn công hơn nhưng file càng lớn càng dễ
bị pha loãng, khó tìm đúng đoạn code thật giữa hàng trăm dòng lịch sử quyết định. **Không đề xuất
xoá toàn bộ** — nhiều comment (vd bug D/A/C ở mục 8, seek-retry ở mục 10) vẫn đang là tài liệu duy
nhất giải thích "tại sao code viết thế này, đừng sửa lại theo bản năng" — chỉ những đoạn thuần tường
thuật ngày tháng đã xong xuôi, không còn ảnh hưởng quyết định code hiện tại, mới thật sự là ứng viên
dọn an toàn.

## 13. Đồng bộ lại tài liệu `readme/` — script re-audit thay vì ước lượng tay (12/07/2026)

Thay vì chỉ ghi nhận "tài liệu tụt hậu" làm nợ, đã thực sự chạy lại:

- **Script quét cú pháp mới** (brace-matching xác định đúng function TOP-LEVEL của MODULE, bỏ qua
  hàm lồng bên trong hàm khác; loại bỏ comment/string/template trước khi regex) — **validate khớp
  100%** với 2 hàm mẫu còn nguyên trong bản audit gốc (`core/app-cleanup.js::executeAppCleanup`:
  R2=5 cả 2 lần chạy khớp tuyệt đối; `core/audio-engine.js::setupAudioContext`: R2=21, R1-strong,
  R3=`allocateBuffers`/`applyEQPreset`/`initPitchWorker` khớp tuyệt đối) — TRƯỚC khi áp dụng cho
  toàn bộ 56 file, tránh lặp lại kiểu lệch tài liệu/code từng xảy ra ở ver 10.
- **`readme/core-legacy-audit.md` viết lại hoàn toàn** (không phải vá thêm): 66 file `core/`
  (48→66), 56 file sau loại hot-path (38→56), 366 function (294→366). Phát hiện đáng chú ý: tỉ lệ
  vi phạm GIẢM 56%→36% dù số hàm tăng — 132 hàm ở 28 file MỚI (viết SAU khi Rule 1-4 có hiệu lực)
  có **0 vi phạm Rule 2/Rule 1-strong**, chỉ còn 11 vi phạm Rule 3 (core-gọi-core bare-statement,
  tập trung ở chuỗi helper private trong `core/file-manager/photo-ui.js` và 2 lời gọi cross-file
  `core/settings-panel-stack.js → core/slider-panel-scroll.js`) — bằng chứng thực tế đầu tiên cho
  thấy Rule 2/1 đang được tuân thủ tốt ở code mới. Cũng phát hiện 2 file CŨ (đã audit từ trước) có
  hàm MỚI phát sinh ngoài bản gốc: `core/canvas-scene-setup.js` (+`dataURItoBlobUrl`, +`initRubik`,
  cả 2 sạch) và `core/player-controls.js` (+6 hàm, mất 1 hàm cũ `closeSettingsDrawer` — đổi tên/thu
  hẹp thành `forceBackToPlaylistUI`). Thêm mục **Rule 5 sơ bộ** (CHƯA có ở bản gốc vì Rule 5 viết
  sau 10/07) — 20 hàm có `addEventListener`, phân 2 nhóm theo độ tin cậy (khớp diện miễn 5a vs cần
  soát mắt — xem chi tiết trong chính file đó), 4 candidate Rule 5b.
- **`readme/folder-structure.md` viết lại cây thư mục** — thêm toàn bộ `core/file-manager/` (13
  file), `core/generic-drawer.js`/`settings-panel-stack.js`/`slider-panel-scroll.js`, 10 cụm
  `/event/` mới, `components/generic-drawer.js`/`items.js`/`file-manager.js`/`settings/theme.js`/
  `settings/file-manager-section.js`, `subtitle-editor.html`, `service/song-key-cipher.js`,
  `assets/css/slideshow.css`, `readme/song-cover-background-relations.md` (thiếu trong cây cũ).
  Cập nhật bảng version — hàng v12 giờ liệt kê đủ file Thêm/Sửa thay vì gần như rỗng.
- **`readme/where-to-edit.md` thêm 12 hàng** cho File Manager (điều hướng chung, Song, Photo &
  Album, Documents), Generic Drawer, `items.js`, dọn rác, Settings điều hướng ngăn xếp, Theme — và
  SỬA 3 hàng đã lỗi thời: Kiểu xem/Sắp xếp Playlist (từng trỏ `playlist-background.js` — đã tách
  sang `playlist-view.js` từ 07/07), số file `components/settings/*.js` (6→9, có 1 file không mount
  nữa), cụm `subtitleModal` (giờ chỉ toggle nhanh, không còn mở modal sửa — trang riêng đã thay).
  Cũng thêm cảnh báo "2 bản `DEFAULT_VIZ_CONFIG`" vào đúng hàng "thêm trường cấu hình mới".
- `readme/event-bus-flow.md` — [ĐÍNH CHÍNH, xem mục 14] lúc viết mục này (13) từng kết luận KHÔNG
  cần sửa — SAI, đọc lại kỹ hơn lúc audit `/event/` (mục 14) phát hiện sơ đồ tổng quan vẫn ghi
  "Block gate... hiện RỖNG" dù đã có 1 entry thật từ 03/07 — đã sửa 1 dòng.

---

## 14. Audit `/event/` — kiểm tra vi phạm kiến trúc (listener/router/workflow, 12/07/2026)

Quét script (msg.type cross-reference listener↔router toàn project, if/switch điều kiện đọc thẳng
`appState.get()` trong case Router, bare-call multi-step trong case, gọi `taskManager`/core trực
tiếp từ Listener, `setTimeout`/`setInterval` thô ngoài `taskManager`, core file gọi ngược vào
Workflow) + xác nhận thủ công từng candidate (loại hết false-positive: pattern delegation qua bảng
tra id→type, type dựng bằng ternary, tin nhắn `.confirm` gửi lại từ callback modal trong chính
Router — cả 3 đều hợp lệ, KHÔNG phải bug, chỉ là script thô không nhận ra cách viết động).

**Xác nhận SẠCH (không phát hiện gì):**
- 24/24 router đều tự `eventBus.register()` — không router mồ côi.
- 0 router/listener gọi `taskManager` trực tiếp (đúng quy ước Workflow-only).
- 0 message type bị gửi mà không router nào xử lý (không có lỗi gõ `msg.type`, đã quét TOÀN
  project — kể cả nơi Router tự gửi lại `.confirm` từ callback modal — không chỉ riêng Listener).

**6 vi phạm/lệch quy ước THẬT phát hiện, đã đọc code xác nhận (không suy đoán):**

1. **`event/router/visualizer-misc-settings.js :: visualizerMiscSettings.keepScreenOn.change`**
   (dòng 43-51) — viết tay `if (appState.get('vizConfig').keepScreenOn) { ... } else {
   releaseWakeLock(); }` để chọn `requestWakeLock()`/`releaseWakeLock()` — đúng định nghĩa "(C)"
   ở `event-bus-flow.md` mục 4C ("LUÔN qua `VirtualMachineState`, không viết switch/if tay đọc
   `appState`... kể cả chỉ 1 điều kiện/1 đích") — VI PHẠM rõ ràng, không mơ hồ. Sửa đúng: bọc
   `VirtualMachineState.run([{ state: appState.get('vizConfig').keepScreenOn, operation: '===',
   value: true, callback: () => {...} }, { ..., value: false, callback: () => releaseWakeLock() }])`.
2. **Cùng file, case `visualizerMiscSettings.visualizerType.change`** (dòng 33-40) — gọi
   `updateTypeUI()` rồi `saveConfig()` LIÊN TIẾP thẳng trong Router, không qua Workflow — khớp đúng
   chữ Rule B ("≥2 lời gọi side-effect nối tiếp... LUÔN là Workflow, bất kể đơn giản hay phức tạp").
   Mức độ chắc chắn THẤP HƠN case 1 (2 lời gọi không thật sự phụ thuộc kết quả của nhau, chỉ tình cờ
   đứng cạnh nhau sau 1 lần `appState.set`) — cần Giang xác nhận có tính là "phụ thuộc thứ tự" theo
   đúng tinh thần rule hay không trước khi coi là bắt buộc sửa.
3. **`event/listener/sav-logo.js`** (dòng 24) —
   `document.addEventListener('click', () => { if (appState.get('savLogoExpanded')) eventBus.send(...)
   })` — Listener tự đọc `appState` để quyết định CÓ gửi message hay không — vi phạm nguyên văn mục 1
   `event-bus-flow.md` ("KHÔNG đọc `appState` để quyết định gì"). Ảnh hưởng thực tế THẤP (chỉ tránh
   gửi 1 message thừa khi logo đã thu sẵn, Router nhận message đó cũng sẽ set lại đúng giá trị cũ,
   không gây sai lệch), nhưng vẫn là đường lách nguyên tắc — sửa đúng: LUÔN gửi
   `savLogo.expand.set: {expand:false}` vô điều kiện lúc click ra ngoài, để Router/core tự quyết có
   cần làm gì không.
4. **`event/listener/subtitle-editor.js`** (dòng 258, 265) — gọi THẲNG `scrollSliderTo()`
   (`core/slider-panel-scroll.js`) kèm tính toán `Math.max(0, scrollLeft - clientWidth * 0.8)` ngay
   trong 2 handler click nút mũi tên cuộn toolbar — bỏ qua HOÀN TOÀN eventBus/Router/Workflow, không
   có message type nào cho hành động này. Đây là vi phạm RÕ nhất trong 6 mục — Listener vừa tự tính
   toán (nghiệp vụ) vừa gọi thẳng Core, không giống 2 trường hợp miễn trừ đã có tài liệu
   (`hasRealHoverDevice()`/`_updateToolbarArrowState()` cùng file — 2 hàm ĐÓ có comment giải thích
   rõ vì sao đứng ngoài `/event/`, còn `scrollSliderTo()` thì KHÔNG).
5. **`event/workflow/subtitle-editor.js`** — dùng `setTimeout`/`setInterval` THÔ ở **5 chỗ** (dòng
   290, 329 — `_debugLogInterval`, 593, 1006, 1015) thay vì `taskManager`, trong khi
   `readme/task-manager-conventions.md` ghi rõ "TUYỆT ĐỐI cấm... KHÔNG có ngoại lệ" (chỉ 1 ngoại lệ
   ĐÃ BIẾT: `core/tab-hide-reload.js::triggerHideAndReload()`, không bao gồm file này). **Nghiêm
   trọng hơn 1 bậc:** `subtitle-editor.html` **KHÔNG hề nạp `service/task-manager.js`** trong danh
   sách `<script>` của nó — nghĩa là instance `taskManager` global KHÔNG TỒN TẠI trên trang này, file
   này KHÔNG THỂ tuân thủ quy ước dù có muốn, trừ khi thêm `service/task-manager.js` vào
   `subtitle-editor.html` trước. Đây là lỗ hổng kiến trúc thật sự (không phải quên gõ code) — CẦN
   QUYẾT ĐỊNH: (a) thêm `task-manager.js` vào trang + đổi 5 chỗ sang `taskManager.once()`/`addNew()`,
   hoặc (b) chính thức ghi nhận `subtitle-editor.html` là **ngoại lệ THỨ 2** trong
   `task-manager-conventions.md` (nếu lý do "trang độc lập, timer ngắn hạn, không cần pause/resume
   tập trung" đủ thuyết phục) — KHÔNG tự ý chọn hướng nào trong bản audit này.
6. **`core/config.js :: loadConfig()`** (dòng 285) — gọi THẲNG `workflowTheme.refreshThemeCardUI()`
   — CORE gọi NGƯỢC vào WORKFLOW, sai hướng phụ thuộc hoàn toàn (khác hẳn Rule 3 "core gọi core" đã
   biết — đây là core gọi lên tầng ĐIỀU PHỐI). Tìm thấy qua so sánh với 1 pattern TƯƠNG TỰ nhưng ĐÃ
   ĐƯỢC GIẢI THÍCH ở `core/visualizer/draw-visualizer.js` (dòng 137, 150 — cũng core gọi thẳng
   `workflowSlideshow`/`workflowPlaylistScope`, nhưng có comment rõ "cùng quy ước lifecycle boot,
   đứng ngoài `/event/`, xem event-bus-flow.md mục 1" + bản thân file đó đã MIỄN audit hoàn toàn theo
   `core-legacy-audit.md`). `core/config.js::loadConfig()` cũng chạy 1 LẦN lúc boot (cùng tinh thần),
   NHƯNG (a) `core/config.js` KHÔNG nằm trong danh sách hot-path miễn audit, và (b) dòng gọi
   `workflowTheme` KHÔNG có comment nào viện dẫn lý do "boot lifecycle" như 2 chỗ kia — thiếu nhất
   quán trong cách ghi chú, dù bản chất có thể là cùng 1 loại ngoại lệ hợp lý. Đề xuất: nếu Giang xác
   nhận đây đúng là ngoại lệ boot giống `draw-visualizer.js`, nên viết chính thức 1 nguyên tắc chung
   ("hàm core chạy đúng 1 lần lúc `DOMContentLoaded`/`loadConfig()` được miễn Rule 3 cho lời gọi cần
   thiết để hoàn tất khởi động") vào `core-function-conventions.md` thay vì để mỗi chỗ tự giải thích
   rời rạc bằng comment riêng — tránh phải đoán lại từng trường hợp sau này.

**Đã kiểm nhưng KHÔNG tính là vi phạm (có lý do rõ trong code, chỉ ghi nhận để không đọc nhầm lần
sau):** `event/listener/sav-logo.js::hasRealHoverDevice()` (core, gọi thẳng — nhưng đây là quyết
định "đăng ký sự kiện nào" chứ không phải nghiệp vụ, đúng như docstring đầu file tự giải thích, dù
CHƯA có tên trong danh sách 18 ngoại lệ chính thức ở `changelog/v11.md` mục 2 — có thể bổ sung nếu
muốn đầy đủ tuyệt đối); `event/listener/subtitle-editor.js::_updateToolbarArrowState()` (hàm LOCAL
trong chính file, thuần đổi opacity theo layout, tự nhận "CÙNG TINH THẦN
`updateActiveSubtitleLineHighlight()`" — có tiền lệ, có giải thích).

**Đã cập nhật `readme/event-bus-flow.md`** — sơ đồ tổng quan còn ghi "Block gate... hiện RỖNG" dù
`event/block.js` đã có 1 entry thật từ 03/07 (xem mục 3) — sửa lại đúng hiện trạng.

## 15. Audit đầy đủ Rule 5 (5a/5b/5c) — thay bản sơ bộ, đọc hết từng candidate (12/07/2026)

Đã đọc hết 20 hàm `addEventListener` + 4 candidate Rule 5b từ lần quét trước, viết verdict cuối
cùng vào `readme/core-legacy-audit.md` (thay hẳn mục "sơ bộ" cũ) — tóm tắt:

- **Rule 5a:** 14 hàm tuân thủ đầy đủ (file `-ui.js`, dựng modal/drawer/picker thật). Phát hiện
  THÊM 1 diện miễn trừ hợp lý chưa được đặt tên trong Rule 5a gốc — phần tử DOM tạm/vô hình không
  bao giờ gắn vào cây hiển thị (đo layout, giải mã media, kích hoạt tải file) — xác nhận ở 4 file
  (`loader.js::readAudioDuration`, `id3-export.js`, `document-pagination.js`, `document.js`), đề
  xuất bổ sung chính thức vào `core-function-conventions.md` nếu Giang đồng ý. 3 vi phạm 5a còn lại
  (`wakelock.js`, `resume-state-storage.js`, `state-and-video-bg.js`) đều là **core di sản** (có từ
  trước ver 12) — nợ kế thừa, không phải vi phạm mới, không bắt buộc sửa ngay.
- **Rule 5c:** xác nhận 2 vi phạm — `modal-choice.js` (đã biết) và **`core/settings-panel-stack.js`
  MỚI phát hiện, nghiêm trọng hơn** — đây là code MỚI (08/07/2026, Nhóm D), tự tạo panel DOM động
  nhưng thiếu hậu tố `-ui.js`, VÀ docstring tự nhận đúng câu "Core UI THUẦN... KHÔNG thuộc phạm vi
  Rule 1-4" mà chính Rule 5 đã viết là "KHÔNG CÓ CĂN CỨ" — không thuộc diện legacy nên phải sửa,
  không chỉ ghi nhận nợ.
- **Rule 5b:** sau khi đọc kỹ, **cả 4 candidate đều là false positive** (guard clause hợp lệ hoặc
  dùng `dataset` làm dữ liệu sắp xếp, không phải cờ rẽ nhánh) — kết luận Rule 5b sạch 100%.

## 16. Re-audit `/event/` theo chuẩn Workflow mới (mục bổ sung event-bus-flow.md, bao gồm case đã audit)

Sau khi viết lại định nghĩa Workflow trong `event-bus-flow.md` (chuẩn bị state cho Core — dù chỉ 1
lời gọi — CŨNG là Workflow, không có ngoại lệ "1 core thì khỏi cần"), quét lại TOÀN BỘ 24 router
theo tiêu chí mới: case nào vừa `appState.get()` vừa gọi bare Core (không qua `workflowX.method()`
hay `VirtualMachineState.run()`) đều tính vi phạm, kể cả case ĐÃ audit ở mục 14.

**4 case vi phạm xác nhận (1 case đã biết từ mục 14, 3 case MỚI lộ ra nhờ chuẩn siết chặt hơn):**
1. `event/router/visualizer-misc-settings.js :: keepScreenOn.change` — đã ghi ở mục 14 (đọc
   `vizConfig` chọn `requestWakeLock`/`releaseWakeLock`) — nay CŨNG vi phạm tiêu chí mới (đằng nào
   cũng cần Workflow).
2. **`event/router/equalizer-settings.js :: equalizerSettings.band.input`** — đọc
   `appState.get('eqBandNodes')` rồi gọi thẳng `saveConfig()` — trước đây coi guard clause hợp lệ
   (không rẽ nhánh 2 tiến trình), nhưng theo chuẩn MỚI vẫn là Router tự chuẩn bị state cho Core →
   vi phạm.
3. **`event/router/sav-logo.js :: savLogo.expand.toggle`** — `setSavLogoExpanded(!appState.get(
   'savLogoExpanded'))` — đọc đúng 1 key, gọi đúng 1 hàm core — ví dụ MẪU cho chính quy tắc mới vừa
   viết ("dù chỉ 1 core cũng là Workflow nếu cần state").
4. **`event/router/subtitle-modal.js :: subtitleModal.toggleEnabled.click`** —
   `setSubtitlesEnabled(!appState.get('isSubtitlesEnabled'))` — CÙNG hình dạng hệt case 3 (đọc 1
   key, phủ định, gọi 1 setter) — 2 case này cho thấy đây là 1 PATTERN LẶP LẠI (toggle boolean đơn
   giản) chứ không phải cá biệt, gợi ý nên có 1 cách xử lý DÙNG CHUNG (vd 1 hàm Workflow tiện ích
   tái dùng cho mọi toggle dạng này) thay vì tạo riêng 1 Workflow method/file cho từng cụm.

**Quy tắc `appState.get([key1, key2])` dạng mảng — 0 vi phạm, đã soát đúng phạm vi.** Lần đầu quét
theo "2 lời gọi `appState.get(key)` đứng GẦN NHAU trong cùng 1 khối" — Giang chỉ ra phạm vi đếm SAI:
không phải theo "1 lời gọi Core cần mấy state", mà theo "CẢ 1 lần thực thi Workflow cần mấy state",
kể cả khi 2 giá trị đó rẽ ra phục vụ 2 Core KHÁC NHAU (không đứng gần nhau, mỗi core chỉ cần đúng 1
field). Quét lại đúng phạm vi (gộp toàn bộ THÂN 1 method Workflow, không giới hạn khoảng cách ký tự
giữa 2 lời gọi) — **vẫn 0 vi phạm**, không đổi kết quả nhưng cách quét trước đó (chỉ bắt cặp gần
nhau) đúng là có thể bỏ sót nếu 2 lời gọi cách xa nhau trong cùng method — đã sửa lại
`event-bus-flow.md` mục 4B cho đúng phạm vi này.

**Không phát hiện thêm gì khác** ngoài 4 case trên khi áp chuẩn mới — 20/24 router còn lại tiếp tục
sạch theo cả tiêu chí cũ lẫn mới.

## 17. Đã sửa 5 case `#event` vi phạm — mục 14/16 (12/07/2026, phiên sau)

Theo yêu cầu Giang ("trước mắt xử lý hết #event", tạm bỏ qua riêng `core/config.js::loadConfig()`
vì đó là nợ phát sinh do thêm tính năng không tái cấu trúc, không thuộc phạm vi đợt này) — đã sửa
cả 5 case có `msg.type` cụ thể, đúng hình dạng Workflow đã chốt ở mục 4B `event-bus-flow.md`:

- **3 Workflow file MỚI:** `event/workflow/visualizer-misc-settings.js` (2 method:
  `applyVisualizerType()`, `setKeepScreenOn()` — case sau dùng `VirtualMachineState.run()` NGAY
  TRONG Workflow để chọn `requestWakeLock()`/`releaseWakeLock()`, đúng pattern đã có sẵn ở
  `workflowPlaylist.toggleSelectionMode()`), `event/workflow/equalizer-settings.js` (1 method
  `applyBandInput()`, logic giữ nguyên 100% so với bản cũ trong router — chỉ đổi tầng chứa),
  `event/workflow/sav-logo.js` (1 method `toggleExpand()` — KHÔNG cần `VirtualMachineState` vì
  `setSavLogoExpanded(expand)` nhận thẳng boolean, không phải chọn giữa 2 hàm khác nhau).
- **1 method mới thêm vào Workflow có sẵn:** `workflowSubtitleModal.toggleSubtitlesEnabled()`
  (event/workflow/subtitle-modal.js, cùng lý do — không cần VM, `setSubtitlesEnabled()` cũng nhận
  thẳng boolean).
- **4 Router cập nhật** — mỗi case giờ chỉ còn đúng 1 dòng gọi `workflowX.method(msg.payload)`,
  không tự đọc `appState`/gọi Core nữa. Case `savLogo.expand.set` GIỮ NGUYÊN gọi thẳng Core (đúng
  (A), payload đã đủ, không cần đọc `appState`).
- **`index.html`** — thêm 3 dòng `<script>` Workflow mới (đúng thứ tự: Workflow → Router →
  Listener, sau `event/virtual-machine-state.js`), bump `?v=` cache-bust cho 2 file router/workflow
  của `subtitleModal` (nội dung đổi) + 4 file router/workflow mới, cập nhật lại 3 comment giải
  thích trong `index.html` (từng ghi "KHÔNG cần workflow" — nay sai, đã sửa).
- **Xác nhận bằng script re-scan** (không chỉ đọc mắt): chạy lại đúng script đã dùng để PHÁT HIỆN 4
  case này ở mục 16 — kết quả **0 case còn vi phạm** trong cả 24 router. `node --check` sạch toàn
  bộ project (bao gồm 3 file mới + 4 file sửa).

**Chưa đụng, đúng như Giang yêu cầu:** `core/config.js::loadConfig()` (mục 14, #6) — tạm bỏ qua,
KHÔNG tự ý refactor. 4 vị trí vi phạm không gắn `msg.type` cụ thể — xem mục 18 ngay dưới, TẤT CẢ đã
xử lý xong trong cùng phiên (Giang: "sửa nốt đi").

## 18. Đã sửa nốt 4 mục còn lại — mục 14 (12/07/2026, cùng phiên với mục 17)

- **Listener `sav-logo.js` đọc `appState` trước khi gửi** — bỏ điều kiện `if
  (appState.get('savLogoExpanded'))`, LUÔN gửi `savLogo.expand.set: {expand:false}` vô điều kiện
  mỗi lần bấm ra ngoài (`setSavLogoExpanded(false)` tự idempotent, bỏ điều kiện không đổi hành vi
  thực tế).
- **Listener `subtitle-editor.js` gọi thẳng `scrollSliderTo()` (core)** — tách đúng 3 tầng: listener
  giờ chỉ `eventBus.send()` 2 message mới (`subtitleEditor.toolbarScroll.left/right.click`), thêm 2
  case vào `event/router/subtitle-editor.js` (gọi `workflowSubtitleEditor.scrollToolbar(direction)`).
  **Sửa lần 1 (bản đầu) đặt phép tính `Math.max/min` NGAY TRONG Workflow — SAI, Giang chỉ ra: Workflow
  điều phối, KHÔNG tự chứa phép tính.** Sửa lần 2 (đúng): thêm hàm THUẦN
  `getStepScrollTarget(containerEl, direction)` vào `core/slider-panel-scroll.js` (cùng file với
  `getPositionStart()`/`scrollSliderTo()` đã có — đúng tinh thần file này từ đầu: tách riêng hàm
  TÍNH khỏi hàm HÀNH ĐỘNG, không lặp lại kiểu "logic song song" mà chính file đó được tạo ra để
  tránh). `scrollToolbar()` trong Workflow giờ CHỈ gọi 2 hàm Core nối tiếp
  (`getStepScrollTarget()` rồi `scrollSliderTo()`), không tự tính gì. Bump `?v=` cả 4 file
  (`slider-panel-scroll.js` + 3 file cụm `subtitleEditor`) trong CẢ `index.html` lẫn
  `subtitle-editor.html` (file dùng chung cả 2 trang).
- **`core/settings-panel-stack.js` thiếu hậu tố `-ui.js` (Rule 5c)** — ĐỔI TÊN thành
  `core/settings-panel-stack-ui.js` (copy nội dung, xoá file cũ trên đĩa làm việc — **Giang cần tự
  xoá `core/settings-panel-stack.js` ở project thật nếu bản patch không tự động xoá**), sửa lại
  docstring đầu file (bỏ câu "Core UI THUẦN... KHÔNG thuộc phạm vi Rule 1-4" — SAI theo đúng câu
  chữ Rule 5 — thay bằng "tuân thủ đầy đủ Rule 1-4, CỘNG Rule 5"). Cập nhật `<script src>` +
  6 comment tham chiếu tên file trong `index.html` (đều chỉ là prose, không phải code, nhưng sửa
  luôn cho nhất quán vì đang sửa file này). KHÔNG đổi bất kỳ dòng LOGIC nào — thuần đổi tên + docstring.
- **`event/workflow/subtitle-editor.js` dùng `setTimeout`/`setInterval` thô, thiếu `task-manager.js`
  — ĐÃ SỬA THẬT (12/07/2026, đính chính sau khi Giang chỉ ra lý do "ngoại lệ" ban đầu SAI).** Bản
  đầu chọn ghi nhận ngoại lệ thứ 2 (không đổi code), lý do "trang không nạp `event/tab.js` nên mất
  lợi ích `pauseAll()` theo tab-hide" — **SAI**: `service/task-manager.js` không có dependency gì
  (đọc thẳng file xác nhận 0 tham chiếu `appState`/`document`), và kiểm tra lại còn phát hiện
  `event/tab.js` **CHƯA TỪNG gọi `taskManager.pauseAll()`/`resumeAll()`** ở bất kỳ đâu trong TOÀN BỘ
  app (kể cả `index.html`) — 2 hàm đó là dead code, định nghĩa xong không ai gọi. Coupling tôi viện
  dẫn để biện minh không hề tồn tại. Đã sửa THẬT: thêm `<script src="service/task-manager.js">`
  vào `subtitle-editor.html`, đổi cả 5 điểm gọi — `_seekWithRetry()` (setTimeout→`taskManager.
  once()`), `toggleDebugPanel()` (setInterval/clearInterval→`addNew()`+`operator('enabled')`/
  `kill()`, xoá field `_debugLogInterval` không cần nữa), debounce settle của time-picker
  (setTimeout+clearTimeout thủ công→`taskManager.once(fn, ms, tên cố định theo cột hh/mm/ss)` —
  API tự debounce, code còn NGẮN hơn bản cũ), 2 verification timeout trong
  `_startPlaybackWithRetry()` (→`taskManager.once()`). `readme/task-manager-conventions.md` xoá
  hẳn "ngoại lệ thứ 2" vừa thêm hôm nay — quay về đúng 1 ngoại lệ duy nhất
  (`tab-hide-reload.js`) như trước khi có cụm Subtitle Editor. Rủi ro "sát vùng bug seek-trước-rồi-
  phát" vẫn có thật (lý do còn lại, đứng một mình) — nhưng chuyển `setTimeout(fn, ms)` →
  `taskManager.once(fn, ms)` là phép thay thế 1:1 về hành vi (cùng fire-once sau đúng `ms`, không
  đổi logic bên trong callback) nên rủi ro thấp hơn nhiều so với khi bị hiểu nhầm là phải "tích hợp
  cả `tab.js`" như đánh giá sai ban đầu.

**Xác nhận:** `node --check` sạch toàn bộ project (kể cả file đổi tên). Rà lại toàn bộ `<script
src>` trong `index.html` — không còn đường dẫn nào trỏ tới file không tồn tại.

---

## Nợ kỹ thuật để lại — tổng hợp lại 12/07/2026, đối chiếu THẬT với code (không chép lại handoff cũ)

> **[CẬP NHẬT 13/07/2026]** "Fix B" shuffle (mục ngay dưới) và mục 10 (seek-trước-rồi-phát) đã được
> xử lý xong trong mục 19 — không còn là nợ mở, giữ lại đoạn dưới đây NGUYÊN VĂN để thấy rõ lịch sử
> (từng SAI ghi "đã xong" ở bản 09/07, rồi phát hiện lại CHƯA làm ở bản 12/07, rồi mới thật sự làm
> ở mục 19 ngày 13/07).

**Đính chính so với `sav12-handoff-plan.md` (bản 09/07/2026):**
- **Mục 6 "Fix B" (shuffle dùng đúng `shuffleIndices.length` + giới hạn cứng trong section) —
  bản 09/07 ghi "ĐÃ XONG", nhưng đọc lại `core/player-controls.js::playNext()`/`playPrev()` (dòng
  47-80) xác nhận **CHƯA LÀM** — vẫn dùng `appState.get('playlistOrder').length - 1` làm biên cho cả
  2 hàm, KHÔNG PHẢI `shuffleIndices.length`, không có hàm tách riêng, không có khái niệm "giới hạn
  cứng trong section". Đoạn comment "QUYẾT ĐỊNH KỸ THUẬT QUAN TRỌNG — GIỮ TỔNG ĐỘ DÀI shuffleIndices
  = topLevelKeys.length" ở `core/playlist/order.js` mà bản 09/07 dẫn ra chỉ mô tả ĐÚNG workaround
  ĐÃ CÓ TỪ TRƯỚC (khiến không crash) — KHÔNG PHẢI "Fix B" thật mà `plan.txt` mục 6 yêu cầu. **Việc
  này VẪN LÀ NỢ KỸ THUẬT MỞ**, chưa từng được code.

**Còn nguyên, xác nhận lại qua code thật (không đổi so với các bản handoff trước):**
- **Nhóm B mục 11** — `isShuffle`/`repeatMode`/`isStatsPanelVisible` vẫn là `STATE` top-level riêng
  trong `service/state.js` (dòng ~162-346), KHÔNG nằm trong `vizConfig` — `core/stats-panel-toggle.js`
  vẫn còn nguyên comment gốc "Trạng thái ẨN/HIỆN không lưu vào vizConfig". CHƯA đụng.
- **Nhóm C mục 9** — `'kenburns'` vẫn còn trong `SLIDESHOW_TRANSITION_TYPES` (`core/file-manager/
  slideshow.js`, độc quyền với 12 transition khác), CHƯA tách thành `slideshowConfig.kenBurnsEnabled`
  riêng.
- **Nhóm C mục 10** — `#bg-caption-frame`/`#bg-caption-text` vẫn 0 kết quả grep trong toàn bộ
  `components/` — Caption ẢNH NỀN vẫn CHƯA có HTML thật, toggle "Show caption" vẫn chạy logic nhưng
  không hiển thị gì (khác `#subtitle-display`/Phụ đề LỜI BÀI HÁT — cái này CÓ HTML thật, đang chạy
  bình thường, đừng nhầm lẫn 2 khái niệm).
- **Theme "Sáng" thật** — vẫn chỉ lưu lựa chọn, chưa áp lại màu app (mục 6).
- **Black Hole hiệu năng khi cuộn** — vẫn treo giữa chừng từ trước 09/07, KHÔNG đụng ở bất kỳ phiên
  nào tính tới 12/07 (`will-change: scroll-position` là biện pháp tạm duy nhất đã áp).
- **`core/modal-choice.js` vi phạm Rule 5c** (thiếu hậu tố `-ui.js`) — ghi nhận từ mục 9, chưa đổi.

**Mới ghi nhận trong bản cập nhật này:**
- Mục 10 — Seek-trước-rồi-phát trong Subtitle Editor, tiến trình UI/region/subtitle line không
  đồng bộ dù đã có cơ chế `_seekWithRetry`/`_startPlaybackWithRetry`.
- Mục 11 — 2 file mồ côi/lạc chỗ (`event/workflow/document-picker.js`,
  `lang/patch/subtitle-editor.html`) — khuyến nghị xoá, chưa xoá trong bản này (chỉ ghi nhận, chờ
  Giang xác nhận trước khi động vào file).
- Mục 12 — dữ liệu tỉ lệ comment/file, để ngỏ cho Giang tự quyết đợt dọn riêng nếu muốn.
- Mục 13 (re-audit) — **Rule 3 mới trong code MỚI (không phải code di sản):**
  `core/file-manager/photo-ui.js` có 9 lời gọi bare-statement giữa các hàm private cùng file
  (`_loadNextMasonryChunk`/`_collapseFarMasonryChunks`/`_fillMasonryTile`/`_observeLazyThumbnail`...
  gọi nhau) và `core/settings-panel-stack.js` có 2 lời gọi cross-file tới
  `core/slider-panel-scroll.js::scrollSliderTo` — cả 2 đều VI PHẠM Rule 3 theo đúng câu chữ (cấm
  tuyệt đối core-gọi-core, không phân biệt cùng file hay khác file) dù code này viết SAU khi Rule 3
  siết chặt (04/07) — ghi nhận nợ kỹ thuật, KHÔNG tự ý refactor trong bản cập nhật changelog này.
- Mục 13 — Rule 5 sơ bộ: 5 hàm KHÔNG có hậu tố `-ui.js` nhưng có `addEventListener` gắn lên phần tử
  NGHI LÀ tĩnh (`core/wakelock.js::requestWakeLock`, `core/resume-state-storage.js::
  applyResumeStateToRam`, `core/state-and-video-bg.js::setupVideoBgSource`,
  `core/playlist/loader.js::readAudioDuration`, `core/playlist/render.js::attachCoverFallback`) —
  CẦN ĐỌC TAY để kết luận có vi phạm Rule 5a thật hay không (xem `core-legacy-audit.md` mục Rule 5).
- Mục 14 (audit `/event/`) — 6 vi phạm ban đầu, nay **CHỈ CÒN 1 MỞ**: (1)(2)(3)(4)(5) đã sửa/xử lý
  (xem mục 16-18: cả 2 case `visualizer-misc-settings.js` — `keepScreenOn.change` VÀ
  `visualizerType.change` — đều đã dời sang `workflowVisualizerMiscSettings`, không phải chỉ 1
  trong 2 như nhầm lẫn ghi ở bản trước của mục này) — CÒN MỞ DUY NHẤT: (6)
  `core/config.js::loadConfig()` gọi ngược vào `workflowTheme` — Giang xác nhận đây là nợ do "thêm
  tính năng không tái cấu trúc", TẠM BỎ QUA theo yêu cầu, không thuộc phạm vi các đợt sửa 12/07.
- **Mục 15 (Rule 5 đầy đủ) — `core/settings-panel-stack.js` vi phạm Rule 5c — ĐÃ SỬA** (mục 18):
  đổi tên thành `core/settings-panel-stack-ui.js`, sửa docstring. Đề xuất bổ sung 1 diện miễn trừ
  MỚI vào Rule 5a (phần tử DOM tạm/vô hình dùng để gọi API trình duyệt) — CHƯA viết chính thức vào
  `core-function-conventions.md`, VẪN chờ Giang chốt (việc khác, không liên quan tới file đã sửa).
- **Mục 16/17 — 5 case `#event` ĐÃ SỬA (12/07/2026, phiên sau)** — không còn là nợ mở:
  `visualizerMiscSettings.keepScreenOn.change`, `visualizerMiscSettings.visualizerType.change`,
  `equalizerSettings.band.input`, `savLogo.expand.toggle`, `subtitleModal.toggleEnabled.click` —
  xem mục 17 để biết chi tiết cách sửa (3 Workflow file MỚI + 1 method thêm vào Workflow có sẵn).
  `core/config.js::loadConfig()` (mục 14, #6) — Giang xác nhận đây là nợ phát sinh do "thêm tính
  năng nhưng không tái cấu trúc lại code", TẠM BỎ QUA, không thuộc phạm vi đợt sửa này.
- **[ĐÃ XỬ LÝ trong bản cập nhật này]** `readme/folder-structure.md` (cây thư mục regenerate lại
  từ `find` thật — 66 file `core/`, 66 file `event/`, thêm toàn bộ Nhóm A/B/C/D còn thiếu),
  `readme/where-to-edit.md` (thêm 12 hàng mới: File Manager Song/Photo/Documents, Generic Drawer,
  `items.js`, Settings điều hướng ngăn xếp, Theme, Slideshow+Ken Burns, Subtitle Editor trang riêng
  — kèm sửa 3 hàng đã lỗi thời: `playlist-background.js` cho Kiểu xem/Sắp xếp, số file
  `components/settings/*.js`, cụm `subtitleModal` tên cũ), `readme/core-legacy-audit.md` (QUÉT LẠI
  bằng script mới — 66→56 file sau loại hot-path, 366 function, validate khớp 100% với 2 hàm mẫu
  của bản gốc trước khi áp dụng toàn bộ; phát hiện thêm: tỉ lệ vi phạm GIẢM 56%→36% dù số hàm tăng
  — code MỚI tuân thủ Rule 1-2 tốt hơn hẳn code cũ; thêm mục Rule 5 sơ bộ — addEventListener/
  classList — CHƯA có trong lần audit gốc vì Rule 5 viết sau). KHÔNG đụng `readme/event-bus-flow.md`
  (nội dung vẫn đúng, không cần sửa — chỉ mô tả cơ chế chung, không liệt kê file cụ thể nên không bị
  lỗi thời theo cách 3 file kia bị).

## 19. Sửa 4 mục theo yêu cầu Giang (13/07/2026) — Fix B shuffle, tách đoạn .txt, phân trang Reader, cuộn subtitle

- **Mục 10 (seek-trước-rồi-phát Subtitle Editor) — Giang xác nhận ĐÃ HẾT** sau khi thêm
  `_primeAudioPlayback()` (bắn play()+pause() ngay lúc waveform 'ready' để "làm nóng" thẻ `<audio>`
  trước lượt seek đầu tiên) — không còn là nợ mở.
- **Fix B shuffle (mục 6 plan.txt, nợ mở từ 03/07) — ĐÃ LÀM THẬT:** `core/player-controls.js::
  playNext()`/`playPrev()` giờ dùng đúng `shuffleIndices.length` làm biên (không còn
  `playlistOrder.length` cố định), VÀ đọc `sectionQueueActive` để giới hạn cứng trong đúng
  `displayOrder.length` (độ dài section) khi có 1 "hiện hành" đang active — không còn tràn sang phần
  `remaining` nối sau. Xem docstring cập nhật ở `core/playlist/order.js::updateShuffleArrayFromQueue()`.
- **Thuật toán tách đoạn `.txt` upload** (`core/file-manager/document.js::
  splitPlainTextIntoParagraphs()`) — chuẩn hoá mọi kiểu xuống dòng (`\r\n`, `\r` riêng lẻ kiểu Mac
  cũ) về `\n`, coi MỌI `\n` đơn lẻ là 1 ranh giới đoạn (không chỉ dòng trống như bản cũ), thêm dấu
  hiệu ranh giới MỚI: chấm câu rồi theo sau tab/≥2 dấu cách (không cần ký tự xuống dòng thật).
- **Phân trang Reader — retry-cut cho khối quá cao** (`core/file-manager/
  document-pagination.js::computeNextDocumentReaderSlot()`) — trước đây 1 khối (`<p>` v.v) tự nó
  cao hơn cả khung đọc vẫn xếp TRỌN vào 1 trang, tràn khỏi khung nhìn thấy (không cuộn được, mất nội
  dung). Giờ THỬ CẮT KÝ TỰ (`splitOversizedBlockToFit()`) khi rơi vào trường hợp đó — nội suy điểm
  cắt theo tỉ lệ chiều cao mục tiêu/chiều cao đo được, đo lại thật bằng DOM, co/giãn lại tới khi vừa
  khít (tối đa 6 lần thử). Cursor mở rộng từ số nguyên đơn thành `{blockIndex, textOffset}` để nhớ
  đang cắt dở ở đâu — tương thích ngược với cursor kiểu number cũ. Đã test bằng fake-DOM harness
  (4 kịch bản: bình thường/khối quá cao/hỗn hợp/giữ nguyên thẻ lồng `<b>` qua điểm cắt) — không mất
  ký tự nào, cấu trúc thẻ vẫn hợp lệ.
- **Reader — dọn/khôi phục DOM theo yêu cầu Giang:** `event/workflow/document-reader.js` — mỗi
  `nextPage()` dọn `html` (giữ nguyên `startCursor`/`endCursor`) của các trang đã đọc quá 10 trang
  phía sau (`_pruneFarBehindPages()`); mỗi `prevPage()` lùi vào vùng đã dọn thì khôi phục lại theo lô
  5 trang từ `startCursor` đã nhớ sẵn (`_restorePageHtmlIfNeeded()`, KHÔNG cần duyệt lại từ đầu tài
  liệu).
- **Subtitle Editor — cuộn tới dòng vừa thêm:** `addNewLine()`/`createLineFromSelection()`
  (`event/workflow/subtitle-editor.js`) gọi thêm `_scrollLineIntoView(newSub.id)` ngay sau
  `_renderLines()` — dùng `_lineCardNodesById` có sẵn + `scrollIntoView({block:'center'})`.

← [Quay lại README](../README.md)
